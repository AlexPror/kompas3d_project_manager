# ⚙️ Логика обработки покупных компонентов

## 📋 Описание
В проектах КОМПАС-3D используются покупные компоненты (теплообменник, вентилятор, блок электропитания), которые имеют свои фиксированные обозначения и не должны переименовываться по стандартной схеме проекта.

## 🎯 Покупные компоненты

### 1. Теплообменник

#### В сборке
- ✅ Обозначение: **НЕ меняется** (сохраняется исходное)
- 📌 Пример: `50.100.2300 2ТГ`

#### В детали теплообменника
- ✅ Обозначение: **Обновляется длина** (L1-300)
- ✅ Переменная L1: **Обновляется** автоматически через каскадное обновление
- 📌 Пример:
  - Исходное: `50.100.2300 2ТГ`
  - L1 = 2600
  - Новая длина: 2600 - 300 = 2300
  - Результат: `50.100.2300 2ТГ`

#### Формат обозначения
```
[WIDTH].[HEIGHT].[LENGTH] [TYPE]
   ↓       ↓         ↓        ↓
  50.    100.    L1-300     2ТГ
```

### 2. Вентилятор

#### В сборке
- ✅ Обозначение: **НЕ меняется**
- 📌 Пример: `ВО-3-450-4/0,18-1500`

#### В детали вентилятора
- ✅ Обозначение: **НЕ меняется**
- 📌 Остается исходное обозначение производителя

### 3. Блок электропитания

#### В сборке
- ✅ Обозначение: **НЕ меняется**
- 📌 Пример: `БП-220-24-5А`

#### В детали блока питания
- ✅ Обозначение: **НЕ меняется**
- 📌 Остается исходное обозначение производителя

## 🔧 Обычные детали (для сравнения)

### Стандартная схема
- ✅ Обозначение: **Обновляется** → `ZVD.LITE.H.B1.XXX`
- 📌 Пример: `ZVD.LITE.160.350.001`

### Корпус короба (особый случай)
- ✅ Обозначение: **Обновляется с L1** → `ZVD.LITE.H.B1.L1.XXX`
- 📌 Пример: `ZVD.LITE.160.350.2600.004`

## 💻 Техническая реализация

### В файле designation_updater_fixed.py

#### 1. Обработка в сборке (ШАГ 2)

```python
# СПЕЦИАЛЬНАЯ ОБРАБОТКА для покупных компонентов
if any(word in p_name.lower() for word in ["теплообменник", "вентилятор", "блок электропитания", "блок питания"]):
    # Для покупных компонентов не меняем базовое обозначение
    new_mark = old_mark  # Оставляем как есть
elif "короба" in p_name.lower():
    new_mark = f"{full_name}.{part_id}"
else:
    new_mark = f"{short_name}.{part_id}"
```

#### 2. Обработка файлов деталей (ШАГ 3)

```python
# СПЕЦИАЛЬНАЯ ОБРАБОТКА для покупных компонентов
if "теплообменник" in filename.lower():
    # Для теплообменника обновляем длину (L1-300)
    heat_length = L1 - 300
    old_parts = old_marking.split()
    if len(old_parts) >= 2 and '.' in old_parts[0]:
        size_parts = old_parts[0].split('.')
        if len(size_parts) == 3:
            size_parts[2] = str(heat_length)
            new_marking = f"{'.'.join(size_parts)} {' '.join(old_parts[1:])}"
        else:
            new_marking = old_marking
    else:
        new_marking = old_marking
    self.logger.info(f"    Теплообменник: '{old_marking}' → '{new_marking}'")
    
elif any(word in filename.lower() for word in ["вентилятор", "блок электропитания", "блок питания"]):
    # Для вентилятора и блока питания - не меняем обозначение
    new_marking = old_marking
    self.logger.info(f"    Покупной компонент (не изменяется): '{old_marking}'")
    
else:
    # Обычная деталь - используем marking из сборки
    new_marking = marking
    self.logger.info(f"    Marking: '{old_marking}' → '{new_marking}'")
```

### В файле cascading_variables_updater.py

Переменная L1 в теплообменнике обновляется автоматически через **каскадное обновление переменных**:

```python
# ЭТАП 2: Копирование переменных в детали
for part_file in part_files:
    # Обновляем переменные в детали из сборки
    for var_name, var_value in all_calculated_vars.items():
        var_in_part = var_coll_part.GetByName(var_name)
        if var_in_part:
            var_in_part.value = var_value
            var_in_part.External = True
```

## 📊 Таблица обработки компонентов

| Компонент | Обозначение в сборке | Обозначение в детали | Переменные |
|-----------|---------------------|---------------------|-----------|
| **Теплообменник** | Не меняется | Обновляется длина (L1-300) | L1 обновляется |
| **Вентилятор** | Не меняется | Не меняется | Не обновляются |
| **Блок питания** | Не меняется | Не меняется | Не обновляются |
| **Обычные детали** | Обновляется | Обновляется | H, B1, L1, A1-A5, B3-B6 |
| **Корпус короба** | Обновляется (с L1) | Обновляется (с L1) | H, B1, L1, A1-A5, B3-B6 |

## 🔍 Примеры

### Пример 1: Обновление проекта с теплообменником

**Исходные данные:**
- L1 = 2600
- Обозначение теплообменника: `50.100.2300 2ТГ`

**Процесс:**
1. ШАГ 2 (Обновление marking в сборке): обозначение остается `50.100.2300 2ТГ`
2. ШАГ 3 (Обновление переменных): L1 в детали теплообменника обновляется на 2600
3. ШАГ 4 (Обновление marking в детали): 
   - Расчет: heat_length = 2600 - 300 = 2300
   - Обозначение обновляется: `50.100.2300 2ТГ`

### Пример 2: Вентилятор

**Исходные данные:**
- Обозначение вентилятора: `ВО-3-450-4/0,18-1500`

**Процесс:**
1. ШАГ 2: обозначение остается `ВО-3-450-4/0,18-1500`
2. ШАГ 4: обозначение остается `ВО-3-450-4/0,18-1500`

### Пример 3: Обычная деталь (Распорка)

**Исходные данные:**
- H = 160, B1 = 350, L1 = 2600
- Номер детали: 005

**Процесс:**
1. ШАГ 2: обозначение обновляется на `ZVD.LITE.160.350.005`
2. ШАГ 4: обозначение обновляется на `ZVD.LITE.160.350.005`

## 🧪 Тестирование

Для проверки логики используется регулярное выражение:

```python
# Проверка покупных компонентов
is_purchased = any(word in name.lower() for word in ["теплообменник", "вентилятор", "блок электропитания", "блок питания"])
```

**Тестовые случаи:**
- ✅ "Теплообменник" → Покупной
- ✅ "теплообменник воздушный" → Покупной
- ✅ "Вентилятор" → Покупной
- ✅ "вентилятор осевой" → Покупной
- ✅ "Блок электропитания" → Покупной
- ✅ "блок питания" → Покупной
- ✅ "Корпус короба" → Обычный
- ✅ "Распорка" → Обычный

## 📖 Порядок операций в полном цикле

1. **ШАГ 1**: Копирование проекта
2. **ШАГ 2**: Переименование главной сборки и чертежа
3. **ШАГ 3**: Обновление переменных (включая L1 в теплообменнике)
4. **ШАГ 4**: Обновление обозначений (marking):
   - Покупные компоненты обрабатываются по специальной логике
   - Обычные детали - по стандартной схеме
5. **ШАГ 5-10**: Экспорт DXF, переименование, обновление чертежей, и т.д.

## ⚠️ Важные моменты

1. **Порядок операций критичен**: переменные должны обновляться ПЕРЕД обновлением обозначений
2. **Теплообменник**: единственный покупной компонент, у которого обновляется обозначение (длина)
3. **Переменная L1**: обновляется автоматически во ВСЕХ деталях, включая теплообменник
4. **Регистр не важен**: проверка выполняется через `.lower()`
5. **Ключевые слова**: можно легко добавить новые покупные компоненты в список

## 🔄 Версионирование

- **Версия**: 2.3.0
- **Дата**: 03.11.2025
- **Файлы**: 
  - `components/designation_updater_fixed.py` (строки 231-235, 332-353)
  - `components/cascading_variables_updater.py` (универсальное обновление переменных)

---

**Документация актуальна для версии 2.3.0**












